import { ReactElement } from "react";
/**
 * This is a Standard Panel with an error status, with a prepared message.
 */
declare function ONSErrorPanel(): ReactElement;
export default ONSErrorPanel;
